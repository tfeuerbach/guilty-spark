# Guilty Spark — Route shell history to dedicated log file
# Installed to /etc/rsyslog.d/ by setup.sh
# Captures output from the guilty-spark-logger.sh PROMPT_COMMAND / precmd hooks.

local6.*    /var/log/shell-history.log
& stop
