# Route guilty-spark logs to dedicated files.
# Installed to /etc/rsyslog.d/ by setup.sh (00- prefix for load order).

# Shell history (local6)
local6.*    /var/log/shell-history.log
& stop

# Snoopy Logger (execve capture)
:programname, isequal, "snoopy"    /var/log/snoopy.log
& stop
